Include ~/.orbstack/ssh/config*

# 全局配置（对所有Host生效）
Host *
  # 禁用严格主机密钥检查，无需手动输入yes
  StrictHostKeyChecking no
  # 不存储已知主机密钥（空路径）
  UserKnownHostsFile /dev/null
  # Agent Forwarding
  ForwardAgent yes
  # 保持连接活跃，防止超时断开
  ServerAliveInterval 60
  ServerAliveCountMax 3

# # 连接到Mac的配置
# Host mac
#   HostName MacBook-Pro.local
#   Port 22
#   User ouyangzhaoxin

# # 连接OrbStack的Ubuntu
# Host ubuntu
#   HostName 127.0.0.1
#   Port 32222
#   User ubuntu
#   ProxyJump mac

# # 连接OrbStack的Fedora
# Host fedora
#   HostName 127.0.0.1
#   Port 32222  
#   User fedora
#   ProxyJump mac

Host github.com
    Hostname ssh.github.com
    Port 443
    User git

Host orb
    Hostname orb    
    Port 22
    User default

