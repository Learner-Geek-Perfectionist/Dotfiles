# 所有文本文件用 VS Code 打开（而非弹出"执行"确认框）
# text/* 覆盖 text/plain, text/x-shellscript, text/x-python 等
# 注意: 必须用 open -a 而非裸 code，因为 Kitty 从 GUI 启动时 PATH 不含 VSCode bin
protocol file
mime text/*
action launch --type=background -- open -a "Visual Studio Code" ${FILE_PATH}
